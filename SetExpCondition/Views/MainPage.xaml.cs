namespace SetExpCondition.Views;

public partial class MainPage : ContentPage
{
    public MainPage()
    {
        InitializeComponent();
        // XAMLで BindingContext を指定済みなので、ここで何もする必要はありません。
    }
}
